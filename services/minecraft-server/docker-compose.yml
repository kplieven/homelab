version: "3.8"

services:
  mc:
    image: itzg/minecraft-server
    container_name: mc
    ports:
      - "25565:25565"
    volumes:
      - ./data:/data
    environment:
      EULA: TRUE
    mem_limit: 1500m
    restart: unless-stopped

