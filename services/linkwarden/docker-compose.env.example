# The URL for NextAuth authentication callbacks (port must match PORT variable in .env!)
NEXTAUTH_URL=http://localhost:3333/api/v1/auth
# Secret key for NextAuth session encryption (generate a random string)
NEXTAUTH_SECRET=
# MeiliSearch Settings
MEILI_MASTER_KEY=
